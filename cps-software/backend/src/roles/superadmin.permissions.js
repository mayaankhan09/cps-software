// This file will define what a Superadmin (the platform owner, who manages
// the entire system across all schools) is allowed to do.
// It is empty for now — this is just a placeholder for Phase 0.
// Later, we will fill this array with permission strings like "manage_schools".

const superadminPermissions = [];

export default superadminPermissions;
